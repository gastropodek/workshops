import React from 'react';
import { Text, View } from 'react-native';

export default function FirstComponent () {
    let myName = "Ania";
        return (
            <view>
                <text> Moje imię to: {myName} </text>
            </view>

        );
}